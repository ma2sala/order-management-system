{
  "name": "order-management-system",
  "version": "1.0.0",
  "description": "Waiter-to-barista order management system with real-time Socket.io updates, multi-role dashboards, and PostgreSQL database.",
  "main": "src/server.js",
   "scripts": {
    "start": "prisma migrate deploy && node src/server.js",
    "postinstall": "prisma generate",
    "dev": "nodemon src/server.js",
    "seed": "node src/seed.js",
    "prisma:migrate": "prisma migrate dev",
    "prisma:generate": "prisma generate",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.19.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.4.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "prisma": "^5.19.0"
  }
}
